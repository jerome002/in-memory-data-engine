# in-memory-data-engine
A CLI-based backend system that simulates a multi-entity data store (users, tasks, products) using in-memory structures.  The system supports CRUD operations, search, and filtering, while enforcing data validation and uniqueness constraints.
